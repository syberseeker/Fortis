fastapi
uvicorn[standard]
python-multipart
pydantic

chromadb
sentence-transformers
rank-bm25

pymupdf
python-docx
python-pptx
openpyxl

reportlab
markdown-it-py
huggingface_hub>=0.26
matplotlib>=3.11.2

pywebview

# llama-cpp-python is NOT listed here: setup.ps1 / setup.sh install it
# separately, choosing the CPU or CUDA wheel based on GPU detection. Set
# FORTIS_LLM_BACKEND=stub to run the app without it.
